package eighteen;

public class AI {
	// Will need to implement Thread at some point in the future
	
	private Tree minMaxTree;
	private Pieces myColor;
	
	public AI(Pieces color) {
		minMaxTree = new Tree();
		myColor = color;
	}
	
	// Need to define addLevel
	// Pruning
}
